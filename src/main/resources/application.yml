spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/noticia?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC"
    username: root
    password: root

    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#      dialect: "org.hibernate.dialect.MySQL5InnoDBDialect"
      dialect: "org.hibernate.dialect.MySQL8Dialect"
  thymeleaf:
    cache: false




# username: ${username}
#    password: ${password}